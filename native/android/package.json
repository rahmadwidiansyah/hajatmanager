{
  "name": "hajat-manager-android",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "cap:sync": "cap sync",
    "cap:open": "cap open android",
    "cap:build": "cap sync && cd android && ./gradlew assembleRelease",
    "android:release": "npm run cap:build"
  },
  "dependencies": {
    "@capacitor/core": "^6.2.0",
    "@capacitor/android": "^6.2.0",
    "@capacitor/network": "^6.0.2",
    "@capacitor/preferences": "^6.0.3",
    "@capacitor/app": "^6.0.1",
    "@capacitor-community/sqlite": "^6.0.2",
    "jeep-sqlite": "^2.8.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0",
    "vite": "^6.0.0",
    "typescript": "^5.5.0"
  }
}
