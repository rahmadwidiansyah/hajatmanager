# Dev override — bind mount untuk hot reload (dipakai otomatis oleh `docker compose up`)
# Untuk production GHCR pull: `docker compose -f docker-compose.yml up -d` tanpa override ini
services:
  web:
    volumes:
      - .:/app
      - /app/node_modules
      - /app/.next
    environment:
      NODE_ENV: development
