<Window x:Class="HajatManager.Views.EventDetailWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Detail Acara" Height="720" Width="1100"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource SurfaceBrush}">
  <!-- Task 4: Window hanya shell dialog; seluruh UI + logic di EventDetailView.
       Mode dialog: BackBtn collapsed, tutup via window chrome. -->
  <DockPanel Margin="16" MaxWidth="1120">
    <ContentControl x:Name="Host"/>
  </DockPanel>
</Window>
